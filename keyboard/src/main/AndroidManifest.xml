<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:name="android.hardware.type.input_method" android:required="false" />

    <!-- Signature permission guarding the IME shown/hidden broadcast: only an app
         signed with our key (i.e. GamepadMouse) may receive it, and only WE may send it. -->
    <permission
        android:name="com.droidforge.gamepad.permission.KB_IME_STATE"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.droidforge.gamepad.permission.KB_IME_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <service
            android:name=".GamepadKeyboardService"
            android:exported="true"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>
    </application>
</manifest>
