<?xml version="1.0" encoding="utf-8"?>
<!--
  canRequestFilterKeyEvents  → receive gamepad buttons system-wide (onKeyEvent)
  canPerformGestures         → inject taps / swipes (dispatchGesture)
  canRetrieveWindowContent   → NOT requested; we never read screen content (Play policy friendly)
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRequestFilterKeyEvents|flagDefault"
    android:canPerformGestures="true"
    android:canRequestFilterKeyEvents="true"
    android:description="@string/service_description"
    android:notificationTimeout="100"
    android:settingsActivity="com.droidforge.gamepadmouse.ui.MainActivity"
    android:summary="@string/service_summary" />
